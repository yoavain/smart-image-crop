{
  "name": "smart-image-crop",
  "version": "1.0.0",
  "description": "Lerna root monorepo for @yoavain/smart-image-crop",
  "scripts": {
    "install": "lerna bootstrap --force-local",
    "build": "lerna run build",
    "start-demo-browser": "npm run --prefix demo/smart-image-crop-browser-demo start",
    "start-demo-node": "npm run --prefix demo/smart-image-crop-node-demo start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "4.0.0"
  }
}
