{
  "name": "smart-image-crop",
  "version": "1.0.0",
  "description": "Lerna root monorepo for @yoavain/smart-image-crop",
  "scripts": {
    "install": "lerna bootstrap --force-local",
    "build": "lerna run build",
	"start": "npm run --prefix smart-image-crop-tester start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "4.0.0"
  }
}
